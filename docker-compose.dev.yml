services:
  template-mysql:
    ports:
      - "${MYSQL_PORT:-3306}:3306"

  template-server:
    ports:
      - "${TEMPLATE_SERVER_PORT:-8080}:8080"

  template-web:
    ports:
      - "${TEMPLATE_WEB_PORT:-3000}:80"

  template-node:
    ports:
      - "${TEMPLATE_NODE_PORT:-8090}:8090"
