spring:
  datasource:
    url: jdbc:h2:file:./.loom-local/loom;MODE=MySQL;DB_CLOSE_ON_EXIT=FALSE;DATABASE_TO_LOWER=TRUE
    username: sa
    password:

loom:
  node:
    server-token: local-dev-token
